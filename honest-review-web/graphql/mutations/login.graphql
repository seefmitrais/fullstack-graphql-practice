mutation Login($email: String!,$password: String!) {
    login (input:{
      email:$email
      password:$password
    }){
      errors{
        message
        field
      }
      user{
        id
        email
        username
      }
      token
    }
}