mutation Register($username: String!, $password: String!, $email: String!) {
    register (input:{
      username:$username
      email:$email
      password:$password
    }){
      errors{
        message
        field
      }
      user{
        id
        email
        username
      }
      token
    }
}